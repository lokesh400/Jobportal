<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Certificate Details</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100">
    <div class="max-w-3xl mx-auto mt-10 bg-white p-6 shadow-lg rounded-lg">
        <h1 class="text-2xl font-bold mb-4"><%= certificate.name %></h1>
        <p><strong>Worker:</strong> <%= certificate.workerId.name %></p>
        <p><strong>Job Title:</strong> <%= certificate.postId.title %></p>
        <p><strong>Starting Date:</strong> <%= certificate.startingDate.toDateString() %></p>
        <p><strong>Ending Date:</strong> <%= certificate.endingDate.toDateString() %></p>
        <p><strong>Job Location:</strong> <%= certificate.postId.address %>, <%= certificate.postId.pincode %></p>

        <a href="/worker/index" class="mt-4 inline-block bg-blue-500 text-white px-4 py-2 rounded-lg">Back to Dashboard</a>
    </div>
</body>
</html>
