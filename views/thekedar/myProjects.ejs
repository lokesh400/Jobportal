<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Thekedar Projects</title>
    <link rel="stylesheet" href="/styles.css"> <!-- Link to external stylesheet -->
</head>
<body>
    <div class="container">
        <h1><%= thekedar.name %>'s Projects</h1>
        <a href="/thekedar/<%= thekedar._id %>">🔙 Back to Profile</a>

        <h2>Add New Project</h2>
        <form action="/thekedar/<%= thekedar._id %>/projects/add" method="POST">
            <label>Title:</label>
            <input type="text" name="title" required>

            <label>Address:</label>
            <input type="text" name="address" required>

            <label>Pincode:</label>
            <input type="text" name="pincode" required>

            <label>Photo URL:</label>
            <input type="text" name="photo">

            <label>Description:</label>
            <textarea name="description" required></textarea>

            <button type="submit">Add Project</button>
        </form>

        <h2>Existing Projects</h2>
        <% if (projects.length > 0) { %>
            <div class="project-list">
                <% projects.forEach(project => { %>
                    <div class="project-card">
                        <img src="<%= project.photo %>" alt="Project Photo" class="project-img">
                        <h3><%= project.title %></h3>
                        <p><strong>Address:</strong> <%= project.address %></p>
                        <p><strong>Pincode:</strong> <%= project.pincode %></p>
                        <p><strong>Description:</strong> <%= project.description %></p>
                    </div>
                <% }) %>
            </div>
        <% } else { %>
            <p>No projects added yet.</p>
        <% } %>
    </div>
</body>
</html>
